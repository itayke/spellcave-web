// Placeholder shell. The DOM/CSS cave view lands in the View phase (Phase 4 of MIGRATION_PLAN).
// For now this just confirms the ported engine loads in the browser.
export default function App() {
  return (
    <div style={{ color: '#d7d6d3', fontFamily: 'sans-serif', padding: 24 }}>
      <h1 style={{ color: '#ff7011' }}>Spellcave</h1>
      <p>Pure HTML/React migration — engine core ported. View layer coming next.</p>
    </div>
  );
}